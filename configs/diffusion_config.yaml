sampler: ddpm
steps: 1000
noise_schedule: linear
model_mean_type: epsilon 
model_var_type: learned_range
dynamic_threshold: False
clip_denoised: True
rescale_timesteps: False
timestep_respacing: 1000
seed: null # Set for reproducibility. In multi-run sampling, this seed is the starting point for a sequential random state.
